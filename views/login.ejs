<!DOCTYPE html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录页面</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif; /* 使用 Google Fonts 中的 Roboto 字体 */
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: #fff;
            overflow: hidden;
        }
        .container {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 50px rgba(0, 0, 0, 0.3);
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        h1 {
            margin-bottom: 20px;
            font-size: 2.8em;
            text-shadow: 3px 3px 8px rgba(0, 0, 0, 0.5);
            font-weight: 700; /* 加粗标题 */
        }
        a {
            display: inline-block;
            padding: 15px 30px;
            background: #ff6b6b;
            color: #fff;
            text-decoration: none;
            border-radius: 30px;
            transition: background 0.4s, transform 0.4s;
            font-weight: bold;
            font-size: 1.2em;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        a:hover {
            background: #ff4d4d;
            transform: scale(1.1);
        }
        .register-link {
            display: block;
            margin-top: 20px;
            font-size: 1em;
            color: #fff;
            text-decoration: underline;
            transition: color 0.3s;
        }
        .register-link:hover {
            color: #ff6b6b;
        }
        .background-shape {
            position: absolute;
            width: 350px;
            height: 350px;
            background: rgba(255, 255, 255, 0.15);
            border-radius: 50%;
            top: -150px;
            left: -150px;
            animation: move 6s infinite alternate;
            filter: blur(50px);
        }

        /* 新增动态图形 */
        .dynamic-circle {
            position: absolute;
            width: 100px;
            height: 100px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            animation: float 3s infinite alternate;
            opacity: 0.5;
        }

        @keyframes float {
            0% {
                transform: translateY(0);
            }
            100% {
                transform: translateY(-20px);
            }
        }

        @keyframes move {
            0% {
                transform: translate(0, 0);
            }
            100% {
                transform: translate(120px, 80px);
            }
        }
        @media (max-width: 600px) {
            h1 {
                font-size: 2.5em;
            }
            a {
                padding: 12px 25px;
                font-size: 1em;
            }
            .logo {
                width: 80px; /* 在小屏幕上调整图片大小 */
                height: auto; /* 保持宽高比 */
            }
        }
        .logo {
            width: 150px; /* 调整图片大小 */
            height: auto; /* 保持宽高比 */
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="/blog.webp" alt="Logo" class="logo"> <!-- 引入图片 -->
        <h1>欢迎来到登录页面</h1>
        <a href='/auth/facebook'>通过 Facebook 登录</a>
        <a href="https://www.facebook.com/r.php" class="register-link">没有 Facebook 账号？现在注册</a>
        <div class="background-shape"></div>
        
        <!-- 动态图形 -->
        <div class="dynamic-circle" style="top: 50%; left: 10%; animation-delay: 0s;"></div>
        <div class="dynamic-circle" style="top: 20%; right: 15%; animation-delay: 1s;"></div>
        <div class="dynamic-circle" style="top: 70%; left: 50%; animation-delay: 2s;"></div>
        <div class="dynamic-circle" style="top: 30%; left: 30%; animation-delay: 3s;"></div>
    </div>
</body>
</html>